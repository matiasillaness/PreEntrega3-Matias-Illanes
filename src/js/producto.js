

let carProducts = [];
class Productos {

    constructor(id, nombre, descripcion, precio, cantidad, stock, img) {
        id = this.id
        nombre = this.nombre
        descripcion = this.descripcion
        precio = this.precio
        cantidad = this.cantidad
        stock = this.stock
        img = this.img
    }
}